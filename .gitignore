# Local runtime / control-plane noise
.pcmmad_sync_runs/
**/__pycache__/
*.py[cod]

# Models / caches / checkpoints / executed weight artifacts
models/
model_cache/
.hf_cache/
checkpoints/
executed/
*.safetensors
*.gguf
*.bin
*.pt
*.pth

# Heavy R&D / reincarnation packages are release assets, never normal clone payload
*.zip
*.7z
*.tar
*.tar.gz
*.tgz
*.mht
*.mhtml

# Local credentials / environment
.env
.env.*
*.key
*.pem
credentials.*
secrets.*

# Research stdout/stderr/PID evidence is intentionally tracked under state/ and research/.
# Only local control-plane synchronization noise stays ignored.
