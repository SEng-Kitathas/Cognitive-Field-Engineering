# v1.1 -> v1.2 Branch Reconciliation

Status: **GOVERNANCE / LINEAGE RECONCILIATION; NO NEW SCIENTIFIC OUTCOME**

## Apparent conflict

A later v1.1 hostile overlay stated that joint scientific weight mutation should remain blocked until predicate competence could no longer be satisfied by a one-sided decision basin.

The live project tree also contained an already-preregistered v1.2 factorized primitive-composition branch.

This could look like a contradiction if `joint composition` is treated as one undifferentiated operation.

## Lineage check

The v1.2 preregistration is bound to the earlier six-seed v1.1 hostile closeout SHA:

`1010271f7d244c1a7f8c9450cd9c9b8bdaccb8707643c3d90ea323a9da5fb69f`

That closeout already states that:

- the identifying basis did not cleanly recover the strict boundary;
- the narrow arm usually fell into a `condition_z=true` basin;
- the identifying arm often shifted toward a `condition_z=false/safe` basin;
- the remaining seam is what field geometry yields balanced two-sided predicate behavior.

Therefore v1.2 was not designed under the false assumption that v1.1 had solved the predicate.

## Why v1.2 remains scientifically admissible

v1.2 does **not** train the exact composed mapping `numeric state + mode -> action`.

Instead it separately trains:

1. `numeric state -> condition_z`
2. `condition_z + mode -> opaque action`

with **zero joint numeric-state+mode -> action training examples**.

The primary test then hides `condition_z` and asks whether the two separately trained primitives compose.

The v1.2 preregistration additionally protects against the v1.1 one-sided-basin seam by requiring:

- balanced held-out truth support: 3 false margins / 3 true margins;
- separate `PREDICATE_DIRECT` and `POLICY_DIRECT` readouts;
- composed benefit on both `condition_z=false` and `condition_z=true` pooled slices;
- mean policy-direct accuracy >= 0.95 in both arms;
- positive predicate-direct identifying-minus-narrow mean delta;
- explicit `COMPOSITION_PRESENT_BUT_PREDICATE_UNRESOLVED` disposition if composed action improves while predicate-direct remains one-sided.

Thus v1.2 can answer a different question even if predicate acquisition remains imperfect:

> Does broader predicate support alter zero-shot recruitment/composition of separately trained primitives, and can that effect survive truth-side controls?

## Correction to the later overlay

The later phrase `joint scientific weight mutation remains blocked` was too broad.

Correct interpretation:

- **blocked:** directly training the composed answer and then calling success composition;
- **blocked:** claiming a clean predicate/compiler law from the v1.1 aggregate pass;
- **allowed:** the already-preregistered v1.2 factorized primitive-composition screen, because it was designed after the residual predicate asymmetry was known and contains explicit controls for it.

No v1.1 historical result or v1.2 preregistration is rewritten.

## Current v1.2 execution status at reconciliation

Verified present before any v1.2 scientific trained-arm outcome:

- preregistration frozen;
- candidate generated and statically qualified;
- NF4 baseline admission complete;
- scientific input lock sealed with `scientific_training_authorized=false`;
- exact base/runtime reverified;
- tokenizer replay pass;
- fresh two-run one-step repeatability pass;
- inherited profile qualified;
- no live v1.2 scientific process;
- no v1.2 trained-arm output.

The missing gate is final preexecution authorization.

## Portability condition added before v1.2 scientific execution

Because the v1.1 fresh-checkout CRLF/LF seam was discovered before v1.2 scientific training, v1.2 SHALL receive a companion portability seal before final authorization.

This does not mutate the already-frozen v1.2 scientific input lock. It records raw execution identity and canonical CRLF->LF transport identity separately.

## Laws

- `COMPOSITION TEST != COMPOSED-ANSWER TRAINING`
- `PREDICATE ASYMMETRY != COMPOSITION SCREEN INVALID`
- `PREREGISTERED CONTROL FOR KNOWN SEAM != POST-HOC RESCUE`
- `LATER INTERPRETIVE OVERLAY != AUTOMATIC CANCELLATION OF EARLIER VALID PREREGISTRATION`
- `NORMALIZATION_EQUIVALENT != ORIGINAL EXECUTION IDENTITY`
