# v1.0 Mutation Spec — PLANNED / UNEXECUTED AT CHECKPOINT

Source bridge: chat-local `CFE_PCMMAD_MAXIMUM_REINCARNATION_V3_0_2026-08-29.zip`, top-level member `17_V10_MUTATION_SPEC_PLANNED_UNEXECUTED.md`.
Source package SHA-256 previously verified in this session: `4991366d9308b756c8f318a46b16baf5ea3ed40a4584e4a452569d832b5a73f8`.
This project copy is an evidence bridge only; it does not elevate the plan above live execution evidence.

**Authority:** implementation plan only.  
**Do not mark any item complete without live dev-machine readback.**

## M0 — ancestry

- parent: exact sealed v0.9;
- child target: `CFE_RND_V1_0_PREEXECUTION`;
- parent immutable;
- record parent ZIP hash and package/input-lock verification before child mutation.

## M1 — cross-platform canonical serialization

Target the four historical v0.5 generators implicated by Windows pytest.

Desired mechanism:

- emit UTF-8 bytes with explicit LF;
- avoid host-default newline translation;
- keep byte-for-byte deterministic regeneration tests strict;
- invoke child generators using the same qualified interpreter (`sys.executable` or explicit bound path), not a generic PATH `python`.

Attack:

- prove semantic/parsed content unchanged;
- prove manifest hashes describe canonical generated bytes;
- prove 55/55 tests on Windows.

## M2 — warrant/provenance nuisance uniqueness

Inspect the current field generator and compiler before editing. Preserve target relation and causal-cell balance.

Desired property:

```text
private nuisance variant
→ genuine learner-visible outcome-independent surface variation
```

Add exact uniqueness accounting for training and evaluator prompts.

Do not repair only the statistical analysis while leaving the source generator degenerate unless an explicit decision says the duplicated surface is intended.

## M3 — evaluator pseudoreplication attack

Count exact unique prompts/cases by family and by semantic equivalence key. Ensure inferential tests do not inflate evidence by treating deterministic clones as independent observations.

## M4 — deterministic execution

Inspect current supported Torch/HF controls on the actual host.

Desired change from v0.9:

```text
warn-only deterministic fallback
→ fail-closed or explicitly bounded reproducibility contract
```

Pin attention backend deliberately and record why it is selected.

## M5 — repeated one-step host qualification

Run two same-seed minimal training probes on the exact selected profile and longest sealed sequence. Compare preregistered witnesses.

Keep this qualification outside scientific outcome adaptation.

## M6 — bootstrap discipline

Replace blind toolchain upgrades with version-compatible, checked environment setup. Always run `pip check` and capture freeze/environment identity.

## M7 — exhaustive control audit

Encode and enumerate all 1,680 ordered legal four-offset strict scrambles. Report token-length matching and nuisance/cell parity. The currently selected `(0,2,4,6)` should be evaluated, not post-hoc selected from scientific outcomes.

## M8 — seed extension preregistration

Write exact stage-1 stop/extend rule before live scientific output.

## M9 — rebuild and qualification

After material generator/compiler changes:

- regenerate source/projections/evals/token references as required;
- rerun shortcut, leakage, ancestry, duplicate, transport, and evaluator checks;
- rerun full tests and compileall;
- create new scientific input lock;
- run hostile package attack;
- seal exact membership and verify extraction.

Any changed scientific input means v0.9 qualification does not transfer automatically.

## M10 — only then model download / live host fit

Fetch exact Mistral revision, verify bytes/tokenizer identity, run preregistered fit profiles, then baseline and scientific arms.
