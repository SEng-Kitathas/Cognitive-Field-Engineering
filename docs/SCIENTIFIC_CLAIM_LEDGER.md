# Scientific Claim Ledger

## Earned / verified
- v1.0 executed six paired seeds under the frozen screen and passed execution-integrity qualification.
- Relational co-visibility geometry changes held-out-domain task performance in a family-dependent way under this regime.
- Six-seed family discordance TREATMENT minus CONTROL: `warrant_vs_taint +43`, `dependency_currentness 0`, `bounded_transport -71`.
- The control is not generically broken; it can substantially outperform treatment.
- v1.0 source/global experience parity does not imply optimizer-visible geometry parity.
- Bounded-transport treatment neighborhoods hold `capacity` and `incoming` fixed, and span boundary margins only `{0,+1}`.
- The intended rule `margin > 0` is observationally indistinguishable on that support from the rival `margin == 1`.
- The v1.0 held-out prompts explicitly state substantial parts of the controlling rule/policy, so held-out-domain success is not equivalent to latent relation transfer.

## Provisional / inferred
- Bounded-transport harm may arise from wrong neighborhood/basis construction, sibling interference, optimizer-field diversity starvation, optimizer-horizon mismatch, or some mechanism not yet modeled.
- A high-quality neighborhood may need to hold nuisance variables stable while giving causal operands enough support to eliminate rival rules.

## Not earned / forbidden promotion
- “CFE works.”
- “The model learned a reusable cognitive archetype.”
- “True-neighborhood K1 is universally superior.”
- “The -71 is definitely diversity starvation.”
- “A new K1/K2/K4 curve would by itself explain the sign flip.”
- “Held-out new domain = latent relation learned.”
